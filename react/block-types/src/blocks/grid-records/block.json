{
  "$schema": "https://schemas.wp.org/trunk/block.json",
  "apiVersion": 3,
  "name": "gateway/grid-records",
  "title": "Grid Records",
  "category": "gateway",
  "icon": "list-view",
  "description": "Renders the records table for a Gateway Grid block. Draft — column configuration is hardcoded (ID, Title, Status) pending a configurable column system.",
  "parent": ["gateway/gateway-grid"],
  "usesContext": ["gateway/totalCount", "gateway/isConfigured"],
  "supports": {
    "html": false,
    "interactivity": true,
    "inserter": true
  },
  "editorScript": "file:./index.js"
}
