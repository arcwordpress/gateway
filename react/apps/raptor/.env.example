# Standalone mode credentials — copy to .env and fill in
VITE_API_URL=http://localhost:8000/wp-json
VITE_VERSION=dev
# Schema URL — in standalone mode the file is served from /public by Vite
# Leave blank to use the default: /schemas/raptor/extension.json
# VITE_SCHEMA_URL=/schemas/raptor/extension.json
