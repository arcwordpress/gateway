{
  "name": "gateway-interactivity",
  "private": true,
  "scripts": {
    "build": "wp-scripts build src/view.js --output-path=build --experimental-modules"
  },
  "devDependencies": {
    "@wordpress/scripts": "^31.0.0"
  }
}